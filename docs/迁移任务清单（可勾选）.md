# 迁移任务清单（可勾选）

适用范围：DCF 采用“控制平台 + OpenClaw 运行时”复合体架构时的对齐治理清单。  
使用方式：每次迭代在对应条目打勾，并附测试与验收证据。

## A. 运行时契约

- [ ] A1. `POST /runtime/tasks` 提交链路可用（含 `X-Contract-Version`）
- [ ] A2. `GET /runtime/tasks/:id` 状态查询可用
- [ ] A3. `GET /runtime/tasks/:id/events` 事件拉取可用
- [ ] A4. `GET /runtime/health` 健康检查可用
- [ ] A5. DCF 启动门禁通过（health + ready skills + find-skills）
- [ ] A6. 无 legacy execute fallback（仅 runtime contract）

## B. 组织治理

- [ ] B1. 一人一父员工约束生效
- [ ] B2. 任务风险分级规则生效（L1-L4）
- [ ] B3. 高风险审批链路可用
- [ ] B4. `trace_id/task_id/employee_id` 审计字段完整
- [ ] B5. 任务状态机转移可验证且无非法跳转

## C. 技能治理

- [ ] C1. 技能分类仅 `general|domain`
- [ ] C2. 技能详情保留 `prompt/skillMarkdown/resources`
- [ ] C3. 后台技能详情可见并与运行态一致
- [ ] C4. 技能沉淀最少包含 `SKILL.md` 内容
- [ ] C5. `find-skills` 已安装并 `ready`

## D. 体验与运维一致性

- [ ] D1. 文档默认端口、脚本默认端口一致（当前基线：8092）
- [ ] D2. `start/check/stop` 脚本互相可用
- [ ] D3. smoke 脚本包含登录鉴权与事件断言
- [ ] D4. README 与实际运行策略一致（无互相冲突描述）
- [ ] D5. 标准文档引用链无断链

## E. 质量门禁

- [ ] E1. `npm test` 全通过
- [ ] E2. `npm run verify:openclaw-alignment` 通过
- [ ] E3. `npm run verify:release` 通过
- [ ] E4. layer boundary 无违规
- [ ] E5. 关键变更有对应测试（TDD 证据）

## F. 变更记录模板

每次迭代请附：

1. 迭代编号：
2. 变更范围（文件/模块）：
3. 勾选条目：
4. 测试命令与结果：
5. 风险与回滚方案：
